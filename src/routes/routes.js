const student = require('./student');
const worksheet = require('./worksheet');
const question = require('./question');


module.exports = {
    student: student,
    worksheet: worksheet,
    question: question,
};